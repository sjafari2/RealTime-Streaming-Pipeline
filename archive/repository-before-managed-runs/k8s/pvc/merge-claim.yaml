apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: merged-data
  namespace: kafkastreamingdata
spec:
  storageClassName: rook-cephfs-central
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi

