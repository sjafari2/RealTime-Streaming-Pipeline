# The former producer-data claim is retained for historical evidence recovery.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: producer-data-ucsd-20260915
  namespace: kafkastreamingdata
spec:
  storageClassName: rook-cephfs-ucsd
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi

