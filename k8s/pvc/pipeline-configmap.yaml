# Shared runtime config; the former central PVC is retained for historical recovery.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pipeline-config-recovery-ucsd-20260915
  namespace: kafkastreamingdata
spec:
  storageClassName: rook-cephfs-ucsd
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi

