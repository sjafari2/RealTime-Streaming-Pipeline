apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: merged-data
  namespace: kafkastreamingdata
spec:
  storageClassName: rook-cephfs-block
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 400Gi

