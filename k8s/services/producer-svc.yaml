apiVersion: v1
kind: Service
metadata:
  name: producer-svc
  namespace: kafkastreamingdata
spec:
  selector:
    app: producer-sts
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

