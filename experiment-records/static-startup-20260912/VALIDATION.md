# Static startup implementation verification

The optional startup fix is applied to the final code folder and passed 145 automated tests with no failures or warnings in the final recorded check. No deployment, Nautilus preparation or performance experiment has been performed for this revision. The user requested confirmation of the exact plan before starting.

The changes give consumers unique per-pod static member identities, require prior group membership to clear, capture the actual complete initial assignment only in an empty preparation, and retain strict map and original pod/resource checks for every later three-consumer stage. Four empty preparations check three consumers, a three-consumer restart, six consumers and return to three. Only the explicit comparison option admits the scale-then-keep pair after all four stages pass. Any failure stops the block; there are no retries.

The automated tests cover client configuration/readiness identity, valid reference capture, incomplete or duplicate ownership, changed pod placement, capture forbidden during production or before an intervention, previous group members clearing or timing out, zero-message evidence, the four-stage default and sequence termination after simulated startup or action failures. The previous measurement/lifecycle/evidence tests also passed. The first final-folder test encountered a filesystem warning when pytest tried to write its cache in the Desktop folder; directing only that test cache to the writable review folder produced the clean recorded verification. No application setting was changed to suppress an error.

Actual broker membership behavior, reproducibility across fresh-topic starts, and a matched live performance pair still require the approved Nautilus preparation sequence. Passing local tests does not establish those live results. The six rejected preparations and sixteen previous performance trials remain unchanged.

The exact plan and launch options are in `experiments/controlled-followup-20260912/STATIC_STARTUP.md` and `static-startup-plan.json`. The new protocol has at most six starts (four empty preparations and two optional performance trials), a 1,200-second cumulative preparation cap, and restores original shared YAML, HPA settings and replica counts. The new pair uses seed 71, 60 partitions, 1,500 target messages/s total, 80% to partition 0, 300 seconds production including 60 seconds warm-up, and 120 seconds drain; scale is requested at production +120 seconds.

Backups of changed original files and the test log are under `/Users/soheila/Documents/ChatGPT/Stream Processing Project/review/static-startup-20260912/`. No raw experiment data was generated by this revision.
