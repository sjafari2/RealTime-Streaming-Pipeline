# Dedicated partition calibration

Run `run-20260912-083417` passed the predeclared checks in `experiments/preliminary-campaign-20260912/single-partition-capacity-plan.json`. The qualification JSON records every check, raw-summary hashes, boundary observations and limitations. The run record contains configuration, evidence reconciliation and the reviewed overview plot.

This observed consumer/node did not keep up with 1,200 admissions/s at 2,000 SHA-256 iterations/message. It does not establish a universal partition capacity or predict a different owner on a different node.
