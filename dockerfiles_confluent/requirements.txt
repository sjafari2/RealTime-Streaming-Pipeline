# Data and scientific computing
pandas>=2.1
numpy
scipy>=1.11
h5py
lz4

# Parquet / columnar IO (engines + common compressions)
pyarrow>=14
fastparquet>=2024.2.0
python-snappy>=0.6.1
zstandard>=0.22.0

# NLP
spacy
nltk

# Graph and community detection
networkx
python-louvain
cdlib

# Visualization and progress
matplotlib
plotly
tqdm

# Kafka (Confluent client for modern brokers)
confluent-kafka>=2.0.0  # ✅ Use this instead of kafka-python

# Parallelism, simulation, and utilities
dask
distributed
faker
pyyaml
pyparsing
click
pytz

# Optional but commonly useful tools
httpx         # Async HTTP requests (like requests, but async)
cachetools    # Lightweight memoization or TTL cache
authlib       # OAuth2/OpenID Connect client + server utilities
attrs         # Declarative class data structures, cleaner than dataclasses

# Web + metrics
flask
prometheus_flask_exporter
prometheus-client
psutil

